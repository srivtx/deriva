export interface DesignProblem {
  id: number
  stage: number
  kind: "canvas" | "estimation" | "requirements" | "critique"
  title: string
  pattern: string
  skill: string
  statement: string
  why: string
  hints: string[]
  requiredNodes?: string[]
  requiredEdges?: [string, string][]
  checklist?: { item: string; required: boolean }[]
  estimation?: { prompt: string; answer: string; formula: string }
  walkthrough: string
}

export const STAGES_DESIGN = [
  { id: 0, name: "Requirement Reflex", desc: "clarify before you draw" },
  { id: 1, name: "API Reflex", desc: "contract before components" },
  { id: 2, name: "Capacity Math", desc: "numbers drive architecture" },
  { id: 3, name: "Component Reflex", desc: "know your building blocks" },
  { id: 4, name: "Naive Design", desc: "one box, feel the strain" },
  { id: 5, name: "Optimization", desc: "kill the bottleneck" },
  { id: 6, name: "Mastery", desc: "full designs, tradeoffs aloud" },
]

export const PROBLEMS_DESIGN: DesignProblem[] = [
  // ══ STAGE 0 — Requirement Reflex ══
  {
    id: 1, stage: 0, kind: "requirements", title: "Clarify: URL Shortener", pattern: "scope-cutting", skill: "functional requirements first",
    statement: "You're asked to 'design a URL shortener like bit.ly'. Before drawing anything, select which requirements are CORE (must-have for MVP).",
    why: "Every interview starts with scope. Seniors cut scope; juniors start drawing. The requirement reflex prevents 20 minutes wasted on features nobody asked for.",
    hints: ["Functional = what the system DOES. Shortening is core. Analytics dashboards are not.","Ask: what is the ONE thing this system must do?","Shorten URL + redirect are the two core functions. Everything else is an extension."],
    checklist: [
      { item: "Given a long URL, return a short URL", required: true },
      { item: "Given a short URL, redirect to the long URL", required: true },
      { item: "Short links should not expire (by default)", required: true },
      { item: "Custom alias support", required: false },
      { item: "Click analytics dashboard", required: false },
      { item: "User accounts and link management", required: false },
      { item: "QR code generation", required: false },
    ],
    walkthrough: "Core: shorten + redirect + persistence. Custom aliases, analytics, accounts, QR codes are all v2. In an interview you SAY this out loud: 'I'll focus on shorten and redirect, and mention extensions at the end.' That sentence alone signals seniority.",
  },
  {
    id: 2, stage: 0, kind: "requirements", title: "Clarify: Chat System", pattern: "scope-cutting", skill: "1:1 vs group decides everything",
    statement: "'Design a chat app.' Select which requirements fundamentally change the architecture (must clarify) vs nice-to-haves.",
    why: "Chat architecture is COMPLETELY different for 1:1 vs group vs channels. WhatsApp (small groups) vs Slack (channels) are different systems. Clarify or fail.",
    hints: ["Group size changes the fan-out math. 1:1 = write once. 500-person group = write 500 times.","Online presence is a separate subsystem with its own tradeoffs.","The MUST-clarify questions: group size? read receipts? media? persistence?"],
    checklist: [
      { item: "1:1 messaging", required: true },
      { item: "Group size limit (3? 256? 100k?)", required: true },
      { item: "Message persistence/history", required: true },
      { item: "Online/offline status (presence)", required: true },
      { item: "Read receipts (blue ticks)", required: false },
      { item: "Voice/video calls", required: false },
      { item: "Stickers and reactions", required: false },
    ],
    walkthrough: "Group size is THE clarifying question. WhatsApp caps at ~1000 because fan-out-on-write dies at scale. Slack channels need fan-out-on-read. If you don't ask, the interviewer assumes you don't know the difference.",
  },
  {
    id: 3, stage: 0, kind: "requirements", title: "Read vs Write Heavy", pattern: "workload-shape", skill: "classify the workload",
    statement: "Classify each system as read-heavy, write-heavy, or balanced: (a) Twitter timeline (b) URL shortener (c) Uber driver location (d) YouTube.",
    why: "Workload shape is the first number that matters. Read-heavy → cache everything. Write-heavy → queue everything. The ratio determines the architecture before any component is drawn.",
    hints: ["URL shortener: redirects (reads) vastly outnumber creations (writes). ~100:1.","Uber driver location: drivers write GPS every 4 seconds. Reads are rarer. Write-heavy.","Twitter: a tweet is written once, read by millions. Extreme read-heavy at the feed."],
    checklist: [
      { item: "Twitter timeline: read-heavy (one tweet, millions of reads)", required: true },
      { item: "URL shortener: read-heavy (~100:1 redirect:create)", required: true },
      { item: "Uber driver location: write-heavy (GPS pings every 4s)", required: true },
      { item: "YouTube: read-heavy (watch >> upload)", required: true },
    ],
    walkthrough: "Every system has a shape. State it in the first 3 minutes: 'This is read-heavy, roughly 100:1, so caching and CDN will be central; writes are rare so we can afford slower write paths.' The interviewer now knows you're designing from data, not vibes.",
  },
  {
    id: 4, stage: 0, kind: "requirements", title: "Consistency vs Availability", pattern: "cap-tradeoff", skill: "pick the guarantee",
    statement: "For each scenario, choose: strong consistency or high availability? (a) Bank balance (b) Social media like-count (c) Inventory in flash sale (d) DNS.",
    why: "CAP theorem in practice: during a network partition you pick C or A. Money needs C. Counts can lag. DNS chose A decades ago.",
    hints: ["If showing a stale value costs money → strong consistency.","If showing a stale value is just cosmetic → availability.","Flash sale inventory is the trap: oversell = disaster, so strong consistency on the count."],
    checklist: [
      { item: "Bank balance: strong consistency (stale = wrong money)", required: true },
      { item: "Like count: availability (nobody notices 1234 vs 1235)", required: true },
      { item: "Flash sale inventory: strong consistency (overselling is catastrophic)", required: true },
      { item: "DNS: availability (eventual consistency, 5-min TTLs fine)", required: true },
    ],
    walkthrough: "The rule: ask 'what happens if two users see different values?' Money breaks. Likes don't. Inventory oversells. Say the tradeoff out loud per subsystem — one system can mix both (strong for payments, eventual for likes).",
  },
  {
    id: 5, stage: 0, kind: "requirements", title: "MVP Cut: Design Uber", pattern: "scope-cutting", skill: "the 80/20 of ride-sharing",
    statement: "'Design Uber.' List the absolute MVP features. Cut everything else.",
    why: "Uber is a dozen systems (matching, pricing, maps, payments, ratings, dispatch). An interview is 45 minutes. The MVP cut proves you can find the spine of a complex product.",
    hints: ["Rider requests ride → matched with driver → trip happens. That spine.","Surge pricing, scheduled rides, split fare, ratings — all extensions.","The spine needs: request, matching, location tracking. Nothing else."],
    checklist: [
      { item: "Rider requests a ride", required: true },
      { item: "Driver location updates (GPS ping)", required: true },
      { item: "Matching: find nearest driver", required: true },
      { item: "Trip state machine (requested→accepted→ongoing→done)", required: true },
      { item: "Surge pricing engine", required: false },
      { item: "Scheduled rides", required: false },
      { item: "In-app chat", required: false },
    ],
    walkthrough: "Spine: request → match → trip. Everything else hangs off it. In interviews: 'Core flow is rider requests, we match nearest driver, trip executes. I'll design those three deeply and list extensions at the end.' MVP-cut is a skill, practiced deliberately.",
  },
  {
    id: 6, stage: 0, kind: "requirements", title: "Entities First: Instagram", pattern: "data-model-first", skill: "nouns before architecture",
    statement: "Before drawing Instagram's architecture, list the core entities (tables/objects) and their key fields.",
    why: "The data model IS the design. User, Post, Follow, Like, Comment — five tables and you understand Instagram. Architects who skip entities build castles on air.",
    hints: ["Find the nouns in the product description: users post photos, follow others, like/comment.","Follow is a relationship entity: (follower_id, followee_id).","Media is separate from Post metadata — photos live in blob storage, DB holds URLs."],
    checklist: [
      { item: "User (id, username, bio)", required: true },
      { item: "Post (id, user_id, media_url, caption, created_at)", required: true },
      { item: "Follow (follower_id, followee_id) — the social graph", required: true },
      { item: "Like (user_id, post_id)", required: true },
      { item: "Payment (id, amount, stripe_token)", required: false },
      { item: "Story (24h expiry) — extension, not core", required: false },
    ],
    walkthrough: "Five entities: User, Post, Follow, Like, Comment. Media files go to blob storage; DB stores URLs. The Follow table is the interesting one — it's a directed graph edge, and every feed query joins through it. Entities first, always.",
  },
  {
    id: 7, stage: 0, kind: "requirements", title: "NFR: The Numbers That Matter", pattern: "non-functional", skill: "latency targets",
    statement: "Order these by strictness of latency requirement: (a) stock exchange (b) messaging app (c) video upload (d) analytics dashboard. Then name the 3 NFRs every design must state.",
    why: "Non-functional requirements are where seniors live. 'Fast' is not a requirement; 'p99 < 200ms' is. Stock exchange: microseconds. Analytics: seconds fine.",
    hints: ["Stock exchange: microseconds. Messaging: ~100ms. Video upload: seconds (async). Dashboard: seconds.","The 3 NFRs: latency (p99), availability (nines), durability (data loss tolerance).","Each 9 of availability = 10x less downtime. 99.9% = 8.7h/year. 99.99% = 52min/year."],
    checklist: [
      { item: "Stock exchange: strictest (microseconds, colocated)", required: true },
      { item: "Messaging: tight (~100-200ms feels instant)", required: true },
      { item: "Video upload: lax (async, progress bar, retry)", required: true },
      { item: "Dashboard: laxest (seconds, cacheable)", required: true },
      { item: "State latency, availability, durability for every design", required: true },
    ],
    walkthrough: "Strictness: exchange > messaging > upload > dashboard. The three NFRs to always state: p99 latency, availability nines, durability. Example: 'URL shortener: p99 < 100ms on redirect, 99.95% available, zero link loss.' That's a requirement; 'should be fast' is not.",
  },

  // ══ STAGE 1 — API Reflex ══
  {
    id: 8, stage: 1, kind: "requirements", title: "REST Contract: URL Shortener", pattern: "api-first", skill: "two endpoints define the system",
    statement: "Define the REST API for a URL shortener: methods, paths, request/response bodies. Two endpoints carry the entire system.",
    why: "API before architecture. The contract forces you to commit to inputs/outputs before being seduced by component diagrams. POST /shorten and GET /{code} — that's the whole API surface.",
    hints: ["Create: POST /api/shorten { long_url } → { short_code, short_url }","Redirect: GET /{code} → 301/302 redirect with Location header.","301 = permanent (browser caches, analytics lost). 302 = temporary (every click hits your server). Choose 302 for analytics."],
    checklist: [
      { item: "POST /api/shorten accepts long_url, returns short_code", required: true },
      { item: "GET /{code} returns 302 redirect to long URL", required: true },
      { item: "Error case: unknown code → 404", required: true },
      { item: "Rate limit headers (429) considered", required: false },
    ],
    walkthrough: "POST /api/shorten {long_url} → 201 {short_code}. GET /{code} → 302 Location: long_url. 404 for unknown codes. The 301-vs-302 choice is a classic: 301 browsers cache permanently (your analytics go blind), 302 every click routes through you. State the choice and why.",
  },
  {
    id: 9, stage: 1, kind: "estimation", title: "Pagination: Cursor vs Offset", pattern: "api-contract", skill: "feeds paginate by cursor",
    statement: "Twitter's feed can't use OFFSET pagination at scale. Explain why, and design the cursor-based alternative.",
    why: "OFFSET 1000000 makes the DB scan and skip a million rows — O(n) per page. Cursor pagination ('give me items older than id=12345') is O(log n). Every feed system uses cursors.",
    hints: ["OFFSET problem: rows inserted while paginating cause duplicates/skips; deep offsets are slow.","Cursor = pointer to last seen item: WHERE id < cursor ORDER BY id DESC LIMIT 20.","Response includes next_cursor. Client sends it back for the next page."],
    estimation: {
      prompt: "Design the cursor pagination response format for a feed API.",
      answer: "{ items: [...], next_cursor: 'post_8234' } — client passes next_cursor to fetch the next page. WHERE id < 8234 ORDER BY id DESC LIMIT 20.",
      formula: "OFFSET cost = O(offset). Cursor cost = O(log n) index lookup.",
    },
    walkthrough: "OFFSET 1M scans a million rows then discards them. Cursor seeks the index directly. Also: new inserts shift offsets (items get skipped); cursors are immune. Format: {items, next_cursor}. This one detail — asked in most feed interviews — separates read-the-book from built-the-thing.",
  },
  {
    id: 10, stage: 1, kind: "requirements", title: "Realtime: WebSocket vs Polling", pattern: "transport-choice", skill: "match transport to update frequency",
    statement: "Chat needs live message delivery. Compare long-polling, WebSocket, and SSE. Pick one and justify.",
    why: "Transport choice is an API-level decision with architecture consequences. WebSocket = persistent bidirectional (chat, games). SSE = server-push only (notifications). Polling = simple but wasteful.",
    hints: ["Polling every 1s = 86,400 requests/day/user for mostly 'nothing new'.","WebSocket: one TCP connection, server pushes instantly. Chat's correct answer.","SSE works when client never sends (live score feeds). Half the complexity of WS."],
    checklist: [
      { item: "Chat: WebSocket (bidirectional, low latency, persistent)", required: true },
      { item: "Notifications feed: SSE acceptable (server-push only)", required: true },
      { item: "Polling: fallback for restrictive networks only", required: true },
      { item: "WebSocket needs sticky sessions or a connection layer — noted", required: false },
    ],
    walkthrough: "WebSocket for chat: one connection per user, server pushes new messages instantly, ~zero waste. The hidden cost: millions of persistent connections need dedicated connection-layer machines (and sticky routing). State that cost — it's where the question goes next.",
  },
  {
    id: 11, stage: 1, kind: "requirements", title: "Upload API: Resumable Large Files", pattern: "api-contract", skill: "chunked uploads",
    statement: "Design the upload API for YouTube: users upload 2GB videos on flaky hotel wifi. Naive single-PUT fails constantly. What's the API shape?",
    why: "Large uploads over flaky networks demand chunking + resumability. This is a real API (S3 multipart, tus protocol). The pattern: initiate → upload parts → complete. Each part independently retryable.",
    hints: ["POST /uploads/initiate → upload_id. Client splits file into 5MB chunks.","PUT /uploads/{id}/parts/{n} per chunk. Retry individual failed parts.","POST /uploads/{id}/complete with part list. Server assembles."],
    checklist: [
      { item: "Initiate: POST /uploads → returns upload_id", required: true },
      { item: "Parts: PUT /uploads/{id}/parts/{n} (idempotent per part)", required: true },
      { item: "Complete: POST /uploads/{id}/complete assembles parts", required: true },
      { item: "Client tracks which parts succeeded; retries only failures", required: true },
    ],
    walkthrough: "Multipart: initiate → parts → complete. A 2GB file = 400 × 5MB parts. Hotel wifi drops at part 287? Resume from 287, not 0. Idempotency: PUT part N twice = same result. This API shape is S3's, GCS's, and every serious upload system's.",
  },
  {
    id: 12, stage: 1, kind: "estimation", title: "Idempotency: Pay Twice?", pattern: "api-contract", skill: "idempotency keys",
    statement: "A payment API times out. The client retries. The user gets charged twice. Design the contract that prevents this — without changing the network.",
    why: "Timeouts are ambiguous: did the server process it or not? Retries are mandatory, so duplicate processing is mandatory unless you design against it. Idempotency keys are THE answer — Stripe's API is the reference.",
    hints: ["Client generates a unique key per logical operation: Idempotency-Key: uuid.","Server stores (key → response). Retry with same key → return stored response, don't reprocess.","Key expires after 24h. Stored atomically with the charge."],
    estimation: {
      prompt: "Write the request flow: first attempt + retry after timeout.",
      answer: "Attempt 1: POST /charge {amount}, Idempotency-Key: k_123 → server processes, stores (k_123 → charge_42), returns. Retry: same request → server finds k_123, returns stored charge_42. No double charge.",
      formula: "Unique(key) + atomic store(key, result) = exactly-once effect over at-least-once delivery.",
    },
    walkthrough: "The insight: you can't make the network exactly-once, so make the SERVER exactly-once. Store (idempotency_key → result); retries return the stored result. Every payment API does this. State it proactively in any design involving money, orders, or emails.",
  },
  {
    id: 13, stage: 1, kind: "requirements", title: "Rate Limit Contract", pattern: "api-contract", skill: "429 + headers",
    statement: "Define the rate-limiting contract for a public API: status codes, headers, and what the client should do on 429.",
    why: "Rate limits are part of the API contract, not an afterthought. X-RateLimit-Remaining, Retry-After, 429 — clients need to know limits to respect them.",
    hints: ["Response headers on EVERY request: X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset.","429 + Retry-After: N tells the client exactly when to retry.","Client-side: exponential backoff with jitter on 429."],
    checklist: [
      { item: "X-RateLimit-Limit: requests allowed per window", required: true },
      { item: "X-RateLimit-Remaining: requests left in current window", required: true },
      { item: "429 response with Retry-After header", required: true },
      { item: "Client implements backoff + jitter", required: false },
    ],
    walkthrough: "Headers on every response let well-behaved clients self-throttle; 429 + Retry-After handles the rest. Mention client backoff-with-jitter to show you've consumed APIs, not just built them. The contract prevents thundering-herd retries.",
  },
  {
    id: 14, stage: 1, kind: "requirements", title: "Versioning Strategy", pattern: "api-contract", skill: "v1 forever",
    statement: "Your API has 10,000 mobile app users who never update. You need a breaking change. How do you version? Compare URL path, header, and query-param versioning.",
    why: "Mobile apps ship versions you can't recall. Breaking changes without versioning = dead users. URL path (/v1/) is the pragmatic standard: visible, cacheable, routable.",
    hints: ["URL path: /v1/users — most common, easiest to route and deprecate.","Header: Accept: application/vnd.api.v2 — clean URLs, invisible to logs.","Sunset policy: announce v1 deprecation date, run both in parallel."],
    checklist: [
      { item: "URL path versioning (/v1/) chosen for visibility", required: true },
      { item: "Both versions run in parallel during migration", required: true },
      { item: "Sunset header announces deprecation", required: true },
      { item: "Query-param versioning (?v=2) — leaks into caches, avoid", required: true },
    ],
    walkthrough: "/v1/ in the path wins on pragmatism: visible in logs, trivially routed at the LB, cache-friendly. Run v1+v2 in parallel, send Sunset headers, monitor v1 traffic to zero, then kill. Never break a client you can't force-update.",
  },

  // ══ STAGE 2 — Capacity Math ══
  {
    id: 15, stage: 2, kind: "estimation", title: "QPS: Twitter Writes", pattern: "back-of-envelope", skill: "DAU → QPS in 3 steps",
    statement: "Estimate tweet write QPS. 200M daily active users, 10% tweet daily, average 2 tweets each.",
    why: "Every design interview requires one estimation. The method matters more than precision: users → actions/day → ÷86,400 seconds → QPS. Peak = 2× average. This is Fermi estimation applied to systems.",
    hints: ["Tweeters per day: 200M × 10% = 20M users.","Tweets per day: 20M × 2 = 40M tweets.","QPS: 40M ÷ 86,400 ≈ 460 average, ~1,000 peak."],
    estimation: {
      prompt: "Tweets per second (average and peak)?",
      answer: "200M DAU × 10% tweet × 2 tweets = 40M tweets/day. 40M ÷ 86,400s ≈ 460 QPS average, ~1,000 QPS peak.",
      formula: "QPS = (users × action_rate × actions) ÷ 86,400. Peak ≈ 2× average.",
    },
    walkthrough: "200M × 0.1 × 2 = 40M tweets/day. Divide by 86,400 (memorize this number — seconds in a day) → ~460 QPS avg, ~1K peak. The READ side is the killer: if each of 200M users reads 50 tweets/day, that's 10B reads/day ≈ 115K QPS. Read:write ≈ 250:1. Now you know where the cache goes.",
  },
  {
    id: 16, stage: 2, kind: "estimation", title: "Storage: 5 Years of Short URLs", pattern: "back-of-envelope", skill: "rows → bytes → drives",
    statement: "URL shortener: 100M new URLs/month. Each record: short_code (7B), long_url (500B avg), created_at (8B), user_id (8B). Estimate 5-year storage.",
    why: "Storage estimation answers 'can one DB hold this?' — the single most common sizing question. Rows × bytes/row × months. Then add indexes (+30%) and replication (×3).",
    hints: ["Bytes per record: 7 + 500 + 8 + 8 ≈ 523B ≈ 0.5KB.","Records in 5 years: 100M × 60 months = 6B records.","Storage: 6B × 0.5KB = 3TB. With indexes+replication: ~12TB. Sharding territory."],
    estimation: {
      prompt: "Total 5-year storage (raw and with replication)?",
      answer: "6B records × ~0.5KB = 3TB raw. With indexes (+30%) and 3× replication: ~12TB. Multiple machines needed → sharding required.",
      formula: "storage = records × bytes_per_record × (1 + index_overhead) × replication_factor",
    },
    walkthrough: "6B rows × 0.5KB = 3TB raw. Indexes +30% → 4TB. 3× replication → 12TB. One big machine could technically hold 12TB, but you wouldn't bet the company on one drive. This number is WHY we shard (Stage 5). Estimation isn't trivia — it's the justification for every box you draw later.",
  },
  {
    id: 17, stage: 2, kind: "estimation", title: "Bandwidth: Video Streaming", pattern: "back-of-envelope", skill: "bitrate × viewers",
    statement: "Estimate egress bandwidth for a video platform: 10M concurrent viewers average, 1080p streams at 5 Mbps each.",
    why: "Bandwidth is where video systems bleed money. Bitrate × concurrency = egress. This number justifies CDNs instantly: 50 Tbps from origin servers is impossible; from 200 edge locations, it's Tuesday.",
    hints: ["Per viewer: 5 Mbps.","10M concurrent × 5 Mbps = 50M Mbps = 50 Tbps.","No origin cluster serves 50 Tbps. That's why CDN edges exist."],
    estimation: {
      prompt: "Total egress bandwidth?",
      answer: "10M viewers × 5 Mbps = 50 Tbps. Impossible from one origin → CDN mandatory. Origin only serves edges; edges serve viewers.",
      formula: "bandwidth = concurrent_viewers × bitrate_per_stream",
    },
    walkthrough: "50 Tbps — say it and watch the interviewer nod. No datacenter serves that; Akamai/CloudFront edges do. Origin → CDN edge (once per video per region), edge → viewers (millions). Adaptive bitrate (240p-4K ladders) reduces the average. The math picked the architecture again.",
  },
  {
    id: 18, stage: 2, kind: "estimation", title: "Cache Sizing: The 80/20 Rule", pattern: "back-of-envelope", skill: "hot-set fits in RAM",
    statement: "URL shortener: 3TB of mappings total, but redirects follow an 80/20 power law. How much RAM caches the hot set? Daily redirects: 10B.",
    why: "Caching works because access is skewed. If 20% of URLs serve 80% of traffic, caching 20% of 3TB = 600GB handles 80% of load. This single calculation is the entire justification for Redis in front of your DB.",
    hints: ["Hot set: 20% of 3TB ≈ 600GB of RAM.","A Redis cluster with 25 × 24GB nodes covers it.","Cache hit 80% → DB load drops from 115K QPS to ~23K QPS."],
    estimation: {
      prompt: "Cache size for 80% hit rate? Resulting DB QPS reduction?",
      answer: "20% of 3TB ≈ 600GB RAM ≈ 25-node Redis cluster. DB load drops 80%: from ~115K to ~23K QPS.",
      formula: "cache_size = total_data × hot_fraction. db_qps = total_qps × (1 - hit_rate)",
    },
    walkthrough: "Power laws are why caches work. 600GB of RAM kills 80% of DB load. The estimation chain: total data → hot fraction → RAM → cluster size → resulting DB QPS. Run this chain out loud and the cache box you draw is justified, not decorative.",
  },
  {
    id: 19, stage: 2, kind: "estimation", title: "WhatsApp Message Volume", pattern: "back-of-envelope", skill: "messages → storage + throughput",
    statement: "WhatsApp-scale: 2B users, 50 messages/day each, average message 100 bytes. Estimate daily message volume and 1-year metadata storage.",
    why: "Chat estimation combines throughput (QPS) and storage. 100B messages/day is ~1.2M msg/sec average — that's the throughput your delivery layer must sustain.",
    hints: ["Messages/day: 2B × 50 = 100B messages.","Throughput: 100B ÷ 86,400 ≈ 1.2M msg/sec average, 2.5M+ peak.","Storage (text only): 100B × 100B = 10TB/day → 3.6PB/year."],
    estimation: {
      prompt: "Messages/day, average msg/sec, 1-year text storage?",
      answer: "100B msgs/day ≈ 1.2M msg/sec avg. Text: 10TB/day ≈ 3.6PB/year. Media excluded (that goes to blob storage, ~100× bigger).",
      formula: "msgs = users × msgs_per_user. storage = msgs × bytes_per_msg.",
    },
    walkthrough: "100B msgs/day, 1.2M/sec sustained — a single server does ~10K msg/sec, so you need hundreds of delivery machines. Text storage 3.6PB/year in the DB; media goes to blob storage separately. WhatsApp ran this on ~50 engineers via Erlang — architecture, not headcount, carries load.",
  },
  {
    id: 20, stage: 2, kind: "estimation", title: "Flash Sale: Spike Math", pattern: "back-of-envelope", skill: "design for peak, not average",
    statement: "Flash sale: 10M users hit BUY at 10:00:00 for 100K items. Estimate the request spike and decide where the queue goes.",
    why: "Average-based design dies at spike. 10M simultaneous requests for 100K items means 99% must get 'sold out' FAST. The queue absorbs the spike; the DB sees only what it can handle.",
    hints: ["Spike: 10M requests in ~10 seconds = 1M RPS instant.","DB can honestly process ~10K writes/sec. Mismatch: 100×.","Queue in front: admission control. Only 100K-ish requests reach the DB; rest get 'sold out' in milliseconds."],
    estimation: {
      prompt: "Peak RPS and DB write QPS — what's the mismatch?",
      answer: "~1M RPS spike vs ~10K writes/sec DB capacity = 100× mismatch. Queue decouples: admits at DB's pace, rejects excess instantly.",
      formula: "spike_rps = concurrent_users ÷ spike_window. queue_throughput = db_capacity.",
    },
    walkthrough: "1M RPS vs 10K DB writes — the queue is the only honest answer. Users enter queue, get position, most receive 'sold out' in seconds (better than hanging). The alternative — letting 1M requests hit the DB — is a self-inflicted DDoS. Spike math: always design for the peak you estimated, not the average.",
  },

  // ══ STAGE 3 — Component Reflex ══
  {
    id: 21, stage: 3, kind: "canvas", title: "Load Balancer: The First Box", pattern: "horizontal-scaling", skill: "one IP, many servers",
    statement: "Your single app server is melting at 50K QPS. On the canvas, add the ONE component that lets you run 10 identical app servers behind one address.",
    why: "The load balancer is the first scaling move in every architecture. One public IP, N private servers. Round-robin, least-connections — the algorithm matters less than the concept: horizontal scale needs a traffic cop.",
    hints: ["Clients can't know about 10 servers — they'd need 10 IPs.","One box terminates the connection and forwards to a healthy backend.","Place it between client and app servers."],
    requiredNodes: ["client", "lb", "server"],
    requiredEdges: [["client", "lb"], ["lb", "server"]],
    walkthrough: "Client → LB → Server ×10. The LB health-checks backends, removes dead ones, spreads load. Also terminates TLS so app servers don't. This is the moment your system stops being one process. Every architecture you'll ever draw starts here.",
  },
  {
    id: 22, stage: 3, kind: "canvas", title: "Cache: The Second Box", pattern: "read-offloading", skill: "serve from RAM, not disk",
    statement: "Your DB is at 90% CPU serving the same 20% of rows over and over. Add the component that intercepts reads before they hit the DB.",
    why: "The cache is the second scaling move in every architecture. 80/20 access patterns mean a small RAM layer can absorb most reads. Redis/Memcached between app and DB is the single highest-leverage box you'll ever add.",
    hints: ["Same query, 100K times/day = 100K DB round-trips for the same answer.","One component stores query results in RAM with a TTL.","Place it between app server and DB. Reads check it FIRST."],
    requiredNodes: ["client", "lb", "server", "cache", "db"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "cache"], ["server", "db"]],
    walkthrough: "App checks cache first (cache-aside pattern): hit → return in <1ms. Miss → query DB, write to cache with TTL, return. The invalidation question ('when does the cache lie?') is where the interview goes next — but the placement is the reflex.",
  },
  {
    id: 23, stage: 3, kind: "canvas", title: "Queue: The Third Box", pattern: "async-decoupling", skill: "absorb spikes, work later",
    statement: "Sending a welcome email takes 500ms and makes signup slow. Users don't need the email THIS request. Add the component that defers the work.",
    why: "The queue is the third scaling move. Anything not needed for the response goes async: emails, image resizing, analytics, notifications. Request → enqueue → respond instantly. Workers drain the queue at their own pace.",
    hints: ["Signup response shouldn't wait for SMTP.","One component holds tasks; separate workers consume them.","App server writes to it and returns 200 immediately."],
    requiredNodes: ["client", "lb", "server", "queue", "worker"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "queue"], ["queue", "worker"]],
    walkthrough: "Client → Server → enqueue(email_task) → 200 OK in 20ms. Worker pulls the task and sends the email in the background. Bonus: if the email service is down, tasks pile up safely instead of crashing signups. Queues turn synchronous failure into asynchronous retry.",
  },
  {
    id: 24, stage: 3, kind: "canvas", title: "CDN: Move Content to the Edge", pattern: "geo-distribution", skill: "static content doesn't travel",
    statement: "Users in Sydney wait 400ms for images served from Virginia. Add the component that serves static content from 200 locations worldwide.",
    why: "Light's speed is the hard limit: Sydney→Virginia is 200ms round trip no matter what. CDNs cache static content (images, JS, video) at edge locations near users. Origin sees ~5% of the traffic it would otherwise.",
    hints: ["Static files don't change per user — perfect cache candidates.","One component has POPs (points of presence) in every region.","Client connects to the NEAREST edge, not your origin."],
    requiredNodes: ["client", "cdn", "server"],
    requiredEdges: [["client", "cdn"], ["cdn", "server"]],
    walkthrough: "Client → nearest CDN edge → (cache miss only) → origin. First Sydney user pulls the image from Virginia once; the next million Sydney users get it from a Sydney edge in 20ms. Video platforms, news sites, app stores — anything with static content at global scale runs this shape.",
  },
  {
    id: 25, stage: 3, kind: "requirements", title: "SQL vs NoSQL: The Decision", pattern: "storage-choice", skill: "schema + scale + query shape",
    statement: "For each use case pick SQL or NoSQL and justify: (a) bank transactions (b) user sessions (c) social graph (d) product catalog with varying attributes (e) time-series metrics.",
    why: "The storage decision constrains everything downstream. ACID + joins + fixed schema → SQL. Massive scale + flexible schema + simple lookups → NoSQL. Sessions → Redis. Graph → graph DB or adjacency lists. Time-series → TSDB.",
    hints: ["Money needs ACID transactions → SQL, no debate.","Sessions: key-value lookups, TTL, ephemeral → Redis.","Catalog with varying attributes per category → document DB (MongoDB)."],
    checklist: [
      { item: "Bank transactions: SQL (ACID non-negotiable)", required: true },
      { item: "Sessions: NoSQL/Redis (k-v, TTL, no joins)", required: true },
      { item: "Social graph: graph-friendly store (edges are the data)", required: true },
      { item: "Product catalog: document DB (flexible schema)", required: true },
      { item: "Time-series metrics: TSDB (append + compress + range scan)", required: true },
    ],
    walkthrough: "Money→SQL. Sessions→Redis. Catalog→document. Metrics→time-series. Graph→graph store. The reflex: state your access patterns ('look up by key' vs 'join across tables' vs 'range over time'), and the DB picks itself. Choosing storage by hype instead of access pattern is the junior tell.",
  },
  {
    id: 26, stage: 3, kind: "requirements", title: "Sharding: Hash vs Range", pattern: "data-partitioning", skill: "split by key",
    statement: "Your users table is 2TB. Split it across 4 DB machines. Compare: hash sharding (user_id % 4) vs range sharding (A-F, G-M...). What breaks in each?",
    why: "Sharding is how one logical table lives on many machines. Hash = even distribution but no range queries. Range = range queries work but hotspots. Consistent hashing = rebalancing without moving everything.",
    hints: ["hash(user_id) % 4: perfectly even, but 'all users from last week' scans ALL shards.","Range A-F: 'users A-F' hits one shard, but a celebrity starting with 'A' hotspots it.","Consistent hashing: ring + virtual nodes → adding a 5th machine moves ~20% of keys, not 100%."],
    checklist: [
      { item: "Hash sharding: even load, loses range queries", required: true },
      { item: "Range sharding: range queries work, hotspot risk", required: true },
      { item: "Consistent hashing: minimal reshuffle on resize", required: true },
      { item: "Resharding pain is why you shard late, not early", required: false },
    ],
    walkthrough: "Hash for evenness, range for locality, consistent hashing for resizable clusters. The deep answer: pick the shard key by your queries. 'Get user X' → shard by user_id. 'Posts by user X ordered by time' → shard by user_id AND co-locate their posts (no cross-shard joins). Shard key = query key.",
  },
  {
    id: 27, stage: 3, kind: "requirements", title: "Replication: Leader-Follower", pattern: "data-durability", skill: "writes to one, reads from many",
    statement: "Design DB replication: 1 primary + 3 replicas. Writes go to primary, reads spread across replicas. What breaks when the primary dies? What is replication lag and when does it matter?",
    why: "Replication solves two things: durability (copy survives a crash) and read scale (3 replicas = 3× read capacity). The cost: lag — a write on the primary takes ms to reach replicas. Read-your-own-write needs sticky reads.",
    hints: ["Async replication: primary returns before replicas confirm → fast writes, possible lag.","Primary dies → promote a replica (failover). The latest async writes might be lost.","User updates profile, refreshes, sees old data = lag. Fix: read your own writes from primary."],
    checklist: [
      { item: "Writes → primary only (single writer avoids conflicts)", required: true },
      { item: "Reads → any replica (scales reads 3×)", required: true },
      { item: "Failover: promote replica on primary death", required: true },
      { item: "Read-your-own-write: read from primary after writing", required: true },
    ],
    walkthrough: "One writer, many readers. Async lag means a fresh write might not be visible on a replica for ~10ms — fix by reading your own writes from the primary. Failover promotes the most-up-to-date replica. This pattern powers 99% of production databases; know it cold.",
  },
  {
    id: 28, stage: 3, kind: "requirements", title: "The Toolbox Checklist", pattern: "component-inventory", skill: "name every box and its job",
    statement: "Rapid fire: for each problem, name the ONE component that fixes it. (a) one server can't handle traffic (b) same DB query 100K×/day (c) 2GB video uploads block requests (d) Sydney users wait 400ms (e) flash sale melts DB.",
    why: "The component reflex is pattern-matching at architecture altitude. Symptom → box. Interviewers give symptoms; you name components. This problem installs the whole toolbox in one pass.",
    hints: ["(a) → LB (b) → cache (c) → queue + workers (d) → CDN (e) → queue as admission control.","Notice: queue appears twice. Async is the most versatile tool you own."],
    checklist: [
      { item: "(a) One server can't handle traffic → Load Balancer", required: true },
      { item: "(b) Repeated DB queries → Cache", required: true },
      { item: "(c) Slow work blocks requests → Queue + Workers", required: true },
      { item: "(d) Geographic latency → CDN", required: true },
      { item: "(e) Write spike → Queue (admission control)", required: true },
    ],
    walkthrough: "LB for scale-out, cache for read-offload, queue for async+spikes, CDN for geo, queue again for admission control. Five boxes solve 90% of scaling problems. Stage 4 shows you WHY each exists — by making you suffer without them first.",
  },

  // ══ STAGE 4 — Naive Design ══
  {
    id: 29, stage: 4, kind: "canvas", title: "URL Shortener v0: One Box", pattern: "naive-monolith", skill: "feel the single point",
    statement: "Design the simplest possible URL shortener: ONE machine running app + database together. Build it on the canvas, then answer: what happens when this machine dies?",
    why: "You must build the naive version to feel why it's wrong. One box = one point of failure, one ceiling, one blast radius. Stage 5 fixes it — but you can't appreciate the fix without naming the pain.",
    hints: ["Client talks directly to one server. That server runs the app AND stores data.","Machine dies → 100% outage. No redundancy.","Also: one machine = one CPU/RAM/disk ceiling."],
    requiredNodes: ["client", "server"],
    requiredEdges: [["client", "server"]],
    walkthrough: "Client → Server (app + DB inside). Works fine at 100 users. Dies completely when the machine dies. Can't serve 10K QPS on one CPU. This is every side project ever — and naming its three failure modes (SPOF, ceiling, blast radius) is the point.",
  },
  {
    id: 30, stage: 4, kind: "canvas", title: "Chat v0: Polling Monolith", pattern: "naive-monolith", skill: "polling wastes everything",
    statement: "Design naive chat: clients poll GET /messages every 2 seconds, one server, one DB. Build it, then compute: 10K online users polling every 2s = how many wasted requests/day?",
    why: "Naive chat teaches two lessons at once: monolith (one box) + polling (wasteful transport). 10K users × poll/2s = 432M requests/day where 99% return 'nothing new'. The waste IS the lesson.",
    hints: ["Client → GET /messages → server → DB. Every 2 seconds.","10K users × 43,200 polls/day each = 432M requests/day.","~99% return empty. You're paying 432M requests for ~4M real messages."],
    requiredNodes: ["client", "server", "db"],
    requiredEdges: [["client", "server"], ["server", "db"]],
    walkthrough: "Client polls → Server → DB, 432M times/day, 99% wasted. The fix preview: WebSocket (one persistent connection, server pushes) kills ~all of that waste. But you had to compute the waste to justify the WebSocket layer. That's Stage 5's move — made credible by this math.",
  },
  {
    id: 31, stage: 4, kind: "canvas", title: "Twitter v0: SELECT * FROM tweets", pattern: "naive-monolith", skill: "the feed query that can't scale",
    statement: "Naive Twitter: one server, one DB. Feed = SELECT tweets WHERE author IN (all followees) ORDER BY time DESC LIMIT 20. Build it and explain why this query dies at scale.",
    why: "The naive feed is a JOIN across follows + tweets + sort — per request, per user, 10B times/day. The compute-per-read is enormous. This exact pain is why Twitter invented fan-out-on-write (Stage 5).",
    hints: ["Feed query: for EACH read, join follows + tweets, sort by time, take 20.","Celebrity with 100M followers = the join explodes.","10B feed reads/day × expensive query = database cremation."],
    requiredNodes: ["client", "server", "db"],
    requiredEdges: [["client", "server"], ["server", "db"]],
    walkthrough: "Every feed read = expensive join + sort on demand. At 10B reads/day this is impossible. The insight that fixes it: PRE-COMPUTE the feed at write time (fan-out), so reads are a simple lookup. Stage 5 builds exactly that — but first, feel why the naive query can't survive.",
  },
  {
    id: 32, stage: 4, kind: "critique", title: "Find the Bottleneck", pattern: "bottleneck-analysis", skill: "trace the request, find the limit",
    statement: "A system: Client → LB → 3 app servers → 1 DB. At 8K QPS, latency goes from 50ms to 3s. App servers idle at 20% CPU. DB at 98% CPU. What's the bottleneck and what are your first two moves?",
    why: "Scaling is sequential bottleneck-hunting. App servers idle + DB melting = the DB is the wall. First moves: (1) add read replicas to offload reads, (2) add cache to avoid the DB entirely for hot keys. Only then shard.",
    hints: ["App servers idle, DB at 98% → the bottleneck is the DB, not compute.","Move 1: read replicas (reads off the primary).","Move 2: cache hot queries (avoid DB entirely for 80% of reads)."],
    estimation: {
      prompt: "If 80% of DB load is reads, what does a cache with 80% hit rate do to DB load?",
      answer: "Reads were 80% of 8K QPS = 6.4K. Cache absorbs 80% of those = 5.1K. DB now sees 8K - 5.1K ≈ 2.9K QPS — a 64% total reduction from one box.",
      formula: "db_load = total × (1 - read_pct × hit_rate)",
    },
    walkthrough: "App servers idle + DB at 98% = DB bottleneck. Move 1: replicas for reads. Move 2: cache for hot keys. NEVER shard first — sharding is the last resort (cross-shard joins, resharding pain). The reflex: measure, find the hot component, offload it, and only then consider the expensive surgery.",
  },
  {
    id: 33, stage: 4, kind: "critique", title: "Single Points of Failure Hunt", pattern: "spof-analysis", skill: "every single box is a future outage",
    statement: "Architecture: 2 LBs (active-passive) → 5 app servers → 1 Redis → 1 DB primary → 3 replicas. List every single point of failure remaining, ranked by blast radius.",
    why: "SPOF-hunting is a learned reflex: scan every component and ask 'if this dies, what stops working?' One Redis = cache outage (DB gets hammered). One DB primary = write outage. Rank by blast radius: what takes the WHOLE system down?",
    hints: ["Redis: dies → all reads hit DB → DB melts. Blast: high.","DB primary: dies → writes stop until failover. Blast: total for writes.","LBs are already redundant (active-passive). App servers are N+2. Those are fine."],
    checklist: [
      { item: "Single Redis — cache outage hammers the DB (add replica or accept miss-storm)", required: true },
      { item: "Single DB primary — write outage until failover (automate failover)", required: true },
      { item: "Failover is manual = downtime measured in human reaction time", required: true },
      { item: "App servers (5) and LBs (2) already redundant — not SPOFs", required: true },
    ],
    walkthrough: "SPOFs: Redis (1) and DB primary (1). Redis death = cache-stampede into the DB. Primary death = write outage until someone promotes a replica. Fixes: Redis replica + automated failover (sentinel), automated DB failover with health checks. The rule: any box with count=1 is a scheduled outage.",
  },
  {
    id: 34, stage: 4, kind: "estimation", title: "The N+1 Disaster", pattern: "query-efficiency", skill: "queries per request must be constant",
    statement: "Your feed endpoint loads 20 posts, then for EACH post queries its author, its like-count, and its comment-count. That's 1 + 20×3 = 61 queries per request. At 10K feed requests/sec, how many DB queries/sec is that? Fix it.",
    why: "N+1 is the silent killer: the endpoint looks fine in code review, then melts the DB in production. 61 queries × 10K req/s = 610K queries/sec — no DB survives. The fix: batch/join/prefetch so queries-per-request is constant.",
    hints: ["61 queries/request × 10K requests/sec = 610K QPS on the DB.","Fix: JOIN authors in the post query. Precompute like/comment counts (denormalize).","Target: 1-3 queries per request, regardless of page size."],
    estimation: {
      prompt: "Queries/sec before and after fixing the N+1?",
      answer: "Before: 61 × 10K = 610K QPS (death). After (join + denormalized counts): ~2 × 10K = 20K QPS (fine). A 30× reduction from fixing one endpoint.",
      formula: "db_qps = queries_per_request × requests_per_sec",
    },
    walkthrough: "61 queries per request is invisible in code ('looks clean!') and fatal at scale: 610K QPS. Fix: JOIN for authors, denormalize counters (likes_count column updated at write time). 610K → 20K QPS. The reflex: count queries per request. If it scales with page size, you have an N+1.",
  },

  // ══ STAGE 5 — Optimization ══
  {
    id: 35, stage: 5, kind: "canvas", title: "URL Shortener v1: Add the Cache", pattern: "read-optimization", skill: "hot links in RAM",
    statement: "Upgrade the naive shortener: 100:1 read:write, 80/20 link popularity. Place the cache so hot redirects never touch the DB. Build the full read path on the canvas.",
    why: "This is the first composed optimization: you know WHY the cache (P18 math, P22 placement) — now draw the production read path. Cache-aside: check Redis, miss → DB → populate Redis → redirect.",
    hints: ["Redirect path: LB → app → Redis (hit? 302) → DB (miss) → Redis.set → 302.","Writes go to DB; cache is read-through/aside.","TTL on entries + cache-aside = the standard pattern."],
    requiredNodes: ["client", "lb", "server", "cache", "db"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "cache"], ["server", "db"]],
    walkthrough: "Read: cache hit → 302 in <1ms. Miss → DB → cache.set(code, url, TTL) → 302. Write: DB only (cache fills lazily on first read). One Redis cluster absorbs 80% of 115K QPS. This IS the production shape of every read-heavy service.",
  },
  {
    id: 36, stage: 5, kind: "canvas", title: "URL Shortener v2: Shard the DB", pattern: "write-scaling", skill: "12TB doesn't fit on one drive",
    statement: "The 5-year math said 12TB with replication. Add sharding to the design: short_code is the shard key. Draw the sharded write path.",
    why: "The Stage 2 estimation (P16) justifies this box. Shard by short_code hash: lookups are O(1) routed, each shard holds a fraction. Consistent hashing lets you add shards without reshuffling everything.",
    hints: ["Shard key = short_code (the lookup key — P26's lesson).","hash(code) → shard N. Each shard is primary+replicas.","App server computes the shard itself (or a routing layer does)."],
    requiredNodes: ["client", "lb", "server", "cache", "db"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "cache"], ["server", "db"]],
    walkthrough: "hash(short_code) % N → shard. Each shard: primary + 2 replicas. Cache stays in front (hot keys still RAM). The full stack: client → LB → app → cache → shard-router → shard. Every box is justified by a number you computed in Stage 2. THAT is system design.",
  },
  {
    id: 37, stage: 5, kind: "canvas", title: "Twitter v1: Fan-out on Write", pattern: "feed-precompute", skill: "write once, read cheap",
    statement: "Fix the naive feed: when Alice tweets, PUSH that tweet into the pre-computed feed list of every follower (cached). Reads become: GET my feed list. Draw the write and read paths.",
    why: "Fan-out-on-write inverts the naive design: expensive work happens ONCE at write time; reads become trivial lookups. Tweet → look up followers → write tweet_id into each follower's feed cache. Read = fetch pre-computed list. This is how Twitter actually works.",
    hints: ["Write path: tweet → find followers → push tweet_id into each follower's feed list (Redis).","Read path: GET feed → fetch pre-computed list. No join, no sort.","Celebrity problem: 100M followers = 100M writes per tweet. Mixed model for celebrities."],
    requiredNodes: ["client", "lb", "server", "queue", "worker", "cache", "db"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "queue"], ["queue", "worker"], ["worker", "cache"], ["server", "cache"], ["server", "db"]],
    walkthrough: "Tweet → enqueue → workers fan out to follower feed-lists (async). Read = O(1) fetch. The celebrity fix: users with >1M followers are pulled at read time instead (hybrid). You derived push vs pull vs hybrid — the classic feed-design conversation, from first principles.",
  },
  {
    id: 38, stage: 5, kind: "canvas", title: "Chat v1: WebSocket + Queue", pattern: "realtime-delivery", skill: "connections and delivery, separated",
    statement: "Upgrade naive chat: WebSocket connection layer for live delivery, queue for write-then-deliver decoupling, DB for history. Draw a message's path from sender to receiver.",
    why: "The composed chat design: connection layer (millions of WebSockets), message service (validates + persists), queue (decouple receive from deliver), and the connection layer pushes to the recipient. Each box has exactly one job.",
    hints: ["Sender → connection layer (WS) → message service → DB (persist) → queue → delivery workers.","Delivery worker looks up WHICH connection server holds the recipient's socket.","Recipient offline → message waits in DB; delivered on reconnect."],
    requiredNodes: ["client", "server", "queue", "worker", "db"],
    requiredEdges: [["client", "server"], ["server", "db"], ["server", "queue"], ["queue", "worker"]],
    walkthrough: "Sender's WS → message service → persist to DB → enqueue → delivery worker → recipient's WS (via connection registry). Offline? Message sits in DB until reconnect. Each component: one job, independently scalable. You just designed WhatsApp's spine.",
  },
  {
    id: 39, stage: 5, kind: "canvas", title: "Video Platform v1: CDN + Async Transcode", pattern: "heavy-asset-pipeline", skill: "upload, transcode, serve at the edge",
    statement: "Design the video pipeline: upload → blob storage → transcode workers (240p-4K) → CDN for playback. Draw both the upload path and the playback path.",
    why: "Heavy-asset systems split into two paths: ingestion (upload, process, store) and distribution (CDN edges). Transcoding is slow and CPU-heavy — classic queue+workers. Playback is static + global — classic CDN. Two patterns, one system.",
    hints: ["Upload: chunked multipart (P11!) → S3 → enqueue transcode job.","Transcode workers: one video → 6 resolutions → back to S3.","Playback: manifest + CDN. Origin only feeds edges on cache miss."],
    requiredNodes: ["client", "server", "queue", "worker", "db", "cdn"],
    requiredEdges: [["client", "server"], ["server", "queue"], ["queue", "worker"], ["client", "cdn"]],
    walkthrough: "Upload: multipart → blob → transcode queue → workers write 240p-4K variants → manifest in DB. Playback: client → CDN edge → (miss → origin). Adaptive bitrate picks quality by bandwidth. You composed multipart upload (P11) + queue (P23) + CDN (P24) into YouTube's skeleton.",
  },
  {
    id: 40, stage: 5, kind: "critique", title: "Cache Stampede Defense", pattern: "cache-failure-modes", skill: "hot key expires, 10K requests pile in",
    statement: "A cached homepage feed expires. In the same millisecond, 10K requests all miss cache and hit the DB simultaneously. The DB falls over. Name and design the fix.",
    why: "Caches fail in specific ways: stampede (thundering herd), penetration (querying keys that don't exist), avalanche (many keys expire together). The fix for stampede: request coalescing / lock — ONE request rebuilds, others wait or serve stale.",
    hints: ["10K misses at once = the herd. Only ONE should rebuild the cache.","Fix 1: mutex/lock per key — first request rebuilds, rest wait.","Fix 2: serve-stale — return the expired value while one request refreshes in background."],
    estimation: {
      prompt: "10K concurrent misses without coalescing vs with it — DB queries in each case?",
      answer: "Without: 10,000 identical DB queries. With request coalescing: exactly 1 query; 9,999 requests share the single rebuild.",
      formula: "coalesced_queries = 1 per key, not 1 per request",
    },
    walkthrough: "Stampede = hot key expires, herd stampedes. Fixes: per-key lock (one rebuilds, others wait), serve-stale (everyone gets old value while one refreshes), probabilistic early-recompute (refresh before expiry). Also: jitter TTLs so keys don't expire together (avalanche). Name these failure modes in interviews — instant seniority signal.",
  },

  // ══ STAGE 6 — Mastery ══
  {
    id: 41, stage: 6, kind: "canvas", title: "FULL: URL Shortener", pattern: "complete-design", skill: "compose every lesson",
    statement: "Full production URL shortener. 100M URLs/month, 10B redirects/day, 100:1 read:write, 5-year storage 12TB. Build the complete architecture: every box justified by a number.",
    why: "The mastery test: compose requirements (P1), API (P8), capacity (P15-P18), components (P21-P27), naive pain (P29), optimizations (P35-P36) into one coherent design where every box traces back to a number.",
    hints: ["Spine: client → LB → app servers → cache → sharded DB.","Writes: DB + async analytics via queue.","Reads: cache-aside, 80% absorbed by Redis."],
    requiredNodes: ["client", "lb", "server", "cache", "db", "queue", "worker"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "cache"], ["server", "db"], ["server", "queue"], ["queue", "worker"]],
    walkthrough: "Client → LB (scale-out) → app servers (stateless) → Redis (600GB hot set, 80% hits) → sharded DB (hash by short_code, primary+replicas) → queue → analytics workers. Every box: LB from P21, cache from P18+P35, sharding from P16+P36, queue from P23. The design is the curriculum, composed.",
  },
  {
    id: 42, stage: 6, kind: "canvas", title: "FULL: Twitter Feed", pattern: "complete-design", skill: "fan-out hybrid at scale",
    statement: "Full Twitter: 200M DAU, 460 write QPS, 115K feed read QPS, celebrities with 100M followers. Design the complete feed system with the celebrity fix.",
    why: "The full feed design composes: requirements (P1-P3), cursor pagination (P9), QPS math (P15), fan-out-on-write (P37), and the hybrid celebrity strategy. This is THE canonical system design interview.",
    hints: ["Normal users: fan-out-on-write (push to follower feeds).","Celebrities (>1M followers): fan-out-on-read (pull at read time, merge).","Feed stored in Redis per user: sorted set of tweet_ids."],
    requiredNodes: ["client", "lb", "server", "queue", "worker", "cache", "db"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "cache"], ["server", "db"], ["server", "queue"], ["queue", "worker"], ["worker", "cache"]],
    walkthrough: "Tweet → DB → fan-out queue → workers push to follower Redis feeds (normal users). Read: merge cached feed + celebrity tweets pulled live. Cursor paginate (P9). 250:1 read:write means the cache IS the system; the DB is backup. You can now whiteboard Twitter from first principles.",
  },
  {
    id: 43, stage: 6, kind: "canvas", title: "FULL: WhatsApp", pattern: "complete-design", skill: "1.2M msg/sec delivery",
    statement: "Full WhatsApp: 2B users, 100B msgs/day, 1.2M msg/sec average. WebSocket delivery, offline storage, group chat (≤256), read receipts optional. Build it.",
    why: "The full chat design composes: transport choice (P10), message volume math (P19), WebSocket+queue design (P38), and group fan-out reasoning (P2). The delivery guarantee (at-least-once + acks) is the deep part.",
    hints: ["Connection layer: millions of WS connections, sharded by user_id.","Message flow: persist → enqueue → deliver → ack.","Offline: messages stored, delivered on reconnect (the double-tick lifecycle)."],
    requiredNodes: ["client", "lb", "server", "queue", "worker", "db", "cache"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "db"], ["server", "queue"], ["queue", "worker"], ["worker", "client"]],
    walkthrough: "WS connection layer (sharded) → message service → DB persist → queue → delivery worker → recipient's connection server → recipient. Ack chain: sent ✓, delivered ✓✓, read ✓✓blue. Groups ≤256 keep fan-out bounded (P2's lesson). Media → blob storage (P6's lesson). Every number from P19 justifies the scale of every layer.",
  },
  {
    id: 44, stage: 6, kind: "canvas", title: "FULL: Rate Limiter", pattern: "complete-design", skill: "token bucket in Redis",
    statement: "Design a rate limiter service: 1000 req/sec per user per endpoint, distributed across 50 app servers. Where does the state live? Which algorithm? Build the architecture.",
    why: "Rate limiting is a standalone system design classic. The core: token bucket per (user, endpoint) in Redis (shared state across all 50 servers). Atomic INCR + EXPIRE via Lua script. Sliding window for smoothness.",
    hints: ["State must be SHARED (50 servers) → Redis, not in-memory.","Token bucket: refill rate r, capacity b. Lua script = atomic check+decrement.","Return 429 + Retry-After (P13's contract) on reject."],
    requiredNodes: ["client", "lb", "server", "cache"],
    requiredEdges: [["client", "lb"], ["lb", "server"], ["server", "cache"]],
    walkthrough: "Every request: app server → Redis Lua script (atomic): if tokens > 0, decrement, allow; else 429 + Retry-After. Algorithms: fixed window (burst at edges), sliding log (precise, memory-heavy), token bucket (the standard). State in Redis = all 50 servers enforce ONE limit. Composes P13 (contract) + P22 (cache as shared state).",
  },
  {
    id: 45, stage: 6, kind: "canvas", title: "FULL: Notification System", pattern: "complete-design", skill: "multi-channel fan-out pipeline",
    statement: "Design notifications: push, SMS, email. 10M notifications/day, priority tiers (OTP > marketing), per-channel rate limits, retry with backoff. Build the pipeline.",
    why: "Notifications are a pure pipeline design: intake → route by channel → per-channel workers → providers (FCM, Twilio, SendGrid). Priority queues, retries, idempotency (P12) — every lesson assembles into one system.",
    hints: ["Intake API → priority queue (OTP jumps the line).","Per-channel workers: FCM (push), Twilio (SMS), SendGrid (email).","Idempotency keys prevent duplicate sends on retry (P12!)."],
    requiredNodes: ["client", "server", "queue", "worker", "db"],
    requiredEdges: [["client", "server"], ["server", "queue"], ["queue", "worker"], ["worker", "db"]],
    walkthrough: "Intake → validate → priority queue → channel router → FCM/Twilio/SendGrid workers → provider → delivery tracking in DB. Retries with exponential backoff + idempotency keys. Priority tiers = separate queues (OTP never waits behind a marketing blast). You just composed P11-P13 + P23 into a real production system.",
  },
]
