// ai-ml question bank — Track: Experiments and evaluation (docs/13).
// EXP-001 … EXP-010, EVAL-001 … EVAL-010.

import { q } from "./questions-math"
import type { AiQuestion } from "../../../schema/question"

export const experimentQuestions: AiQuestion[] = [
  q({
    id: "EXP-001", track: "experiments", lessonId: "ai-ml/03-experiments/the-learning-rate-is-a-system-choice",
    kind: "communication",
    prompt: "Write a one-sentence hypothesis before changing this feature: replace the bigram feature with a trigram feature.",
    contextFixture: { change: "bigram → trigram", target: "bug recall" },
    expectedArtifact: "A falsifiable hypothesis: 'trigrams raise bug recall ≥3 points on the fixed split without hurting precision' — with a number and a metric.",
    rubric: ["Names the metric", "States a number", "Is falsifiable"],
    hints: ["What will you measure?", "How much is worth an experiment?", "A hypothesis you can't disprove is a wish."],
    pattern: "Hypothesis first",
    prerequisites: [],
    nextQuestionId: "EXP-002",
  }),
  q({
    id: "EXP-002", track: "experiments", lessonId: "ai-ml/03-experiments/the-learning-rate-is-a-system-choice",
    kind: "construction",
    prompt: "Select the one variable allowed to change in this experiment: learning rate, feature set, seed, and split are all candidates.",
    contextFixture: { variable: "learning rate", fixed: ["feature set", "seed", "split"] },
    expectedArtifact: "Only the learning rate changes; feature set, seed, and split are frozen so the result is attributable.",
    rubric: ["Picks one variable", "Freezes the rest", "States why attribution needs control"],
    hints: ["How do you know what caused the change?", "Which knobs would contaminate the comparison?", "One cause at a time."],
    pattern: "Controlled comparison",
    prerequisites: ["EXP-001"],
    nextQuestionId: "EXP-003",
  }),
  q({
    id: "EXP-003", track: "experiments", lessonId: "ai-ml/03-experiments/build-a-better-baseline-honestly",
    kind: "debugging",
    prompt: "Baseline scored 0.88 on split A, model scored 0.90 on split B. Detect this invalid comparison and state the rule it broke.",
    contextFixture: { baseline: [0.88, "split A"], model: [0.90, "split B"] },
    expectedArtifact: "Different splits → not comparable: the split must be identical (or the comparison repeated across both).",
    rubric: ["Catches the split change", "Names the comparability rule", "Prescribes the fix"],
    hints: ["What differed besides the model?", "Can 0.88 and 0.90 be ranked?", "Hold the split fixed."],
    pattern: "Honest comparison",
    prerequisites: ["EXP-002"],
    nextQuestionId: "EXP-004",
  }),
  q({
    id: "EXP-004", track: "experiments", lessonId: "ai-ml/03-experiments/reproducibility-is-part-of-correctness",
    kind: "implementation",
    prompt: "Reproduce this run from its config and seed: seed 42, lr 0.01, 100 epochs. What must the runner fix to make replay exact?",
    contextFixture: { seed: 42, lr: 0.01, epochs: 100 },
    expectedArtifact: "Seeding every RNG (numpy, python, shuffle), fixed data order, and deterministic ops — then metrics and artifact hash must match.",
    rubric: ["Seeds all RNGs", "Fixes data order", "Verifies by artifact hash"],
    hints: ["Where does randomness enter besides numpy?", "What shuffles between runs?", "How do you prove two runs are the same run?"],
    pattern: "Reproducibility",
    prerequisites: [],
    nextQuestionId: "EXP-005",
  }),
  q({
    id: "EXP-005", track: "experiments", lessonId: "ai-ml/03-experiments/regularization-is-a-tradeoff",
    kind: "prediction",
    prompt: "Read this learning curve: train and val loss both fall slowly and plateau high. Diagnose underfitting and predict the fix.",
    contextFixture: { train: "plateaus at 1.2", val: "plateaus at 1.3", gap: "small" },
    expectedTraceObservation: "Both losses high and close → underfitting; the fix is more capacity (features, model size, epochs), not more data alone.",
    rubric: ["Sees both losses high", "Reads the small gap", "Chooses capacity over data"],
    hints: ["Is the model even fitting training?", "Is the gap wide or narrow?", "What raises capacity?"],
    pattern: "Underfitting",
    prerequisites: [],
    nextQuestionId: "EXP-006",
  }),
  q({
    id: "EXP-006", track: "experiments", lessonId: "ai-ml/03-experiments/regularization-is-a-tradeoff",
    kind: "prediction",
    prompt: "Read this learning curve: train loss near zero, val loss rising after epoch 20. Diagnose overfitting and predict the fix.",
    contextFixture: { train: "→0", val: "rises after 20", gap: "widens" },
    expectedArtifact: "Overfitting: the growing gap is memorization; fix with regularization, dropout, or more data — and stop at epoch 20.",
    rubric: ["Names the widening gap", "Identifies memorization", "Prescribes regularization or early stop"],
    hints: ["Where does the gap start?", "What is the model doing instead of learning?", "Which knob closes the gap?"],
    pattern: "Overfitting",
    prerequisites: ["THEORY-004"],
    nextQuestionId: "EXP-007",
  }),
  q({
    id: "EXP-007", track: "experiments", lessonId: "ai-ml/03-experiments/build-a-better-baseline-honestly",
    kind: "prediction",
    prompt: "An ablation gains +0.002 F1 at 3× serving cost. Decide whether this improvement is practically meaningful and justify.",
    contextFixture: { gain: 0.002, cost: "3×" },
    expectedArtifact: "Not meaningful: within noise, and the cost ratio makes it a net loss — significance and practical value are both missing.",
    rubric: ["Questions significance", "Weighs the cost", "Rejects the tradeoff"],
    hints: ["Can +0.002 be luck?", "What does 3× cost buy?", "An improvement has a price."],
    pattern: "Practical meaning",
    prerequisites: ["EXP-003"],
    nextQuestionId: "EXP-008",
  }),
  q({
    id: "EXP-008", track: "experiments", lessonId: "ai-ml/03-experiments/build-a-better-baseline-honestly",
    kind: "construction",
    prompt: "Design an ablation for this feature group: text length, word count, and punctuation ratio are added together.",
    contextFixture: { group: ["text length", "word count", "punctuation ratio"], control: "baseline without group" },
    expectedArtifact: "Compare baseline vs all three vs each alone (4 runs, same seed/split): isolate each feature's contribution and the group's joint effect.",
    rubric: ["Keeps baseline", "Runs each feature alone", "Fixes seed and split"],
    hints: ["What do you compare against?", "How do you isolate one feature?", "What stays constant across runs?"],
    pattern: "Ablation",
    prerequisites: ["EXP-002"],
    nextQuestionId: "EXP-009",
  }),
  q({
    id: "EXP-009", track: "experiments", lessonId: "ai-ml/05-failure-lab/find-the-error-family",
    kind: "implementation",
    prompt: "Classify these errors by root cause: too-short tickets, rare vocabulary, duplicate training rows, and a leaked eval row.",
    contextFixture: { errors: ["short", "vocab", "duplicates", "leak"] },
    expectedArtifact: "Four families: data (duplicates/leak) vs representation (vocab) vs coverage (short) — grouping by fix, not by symptom.",
    rubric: ["Groups by root cause", "Separates data from model causes", "Makes families actionable"],
    hints: ["Which errors share a fix?", "Which are data problems vs model problems?", "Group by what you would change."],
    pattern: "Error taxonomy",
    prerequisites: ["MODEL-010"],
    nextQuestionId: "EXP-010",
  }),
  q({
    id: "EXP-010", track: "experiments", lessonId: "ai-ml/05-failure-lab/find-the-error-family",
    kind: "prediction",
    prompt: "From this error taxonomy — 60% vocabulary errors, 25% duplicates, 15% short-text — choose the next experiment and predict its effect.",
    contextFixture: { vocab: 60, duplicates: 25, short: 15 },
    expectedArtifact: "Attack vocabulary first: a larger/regularized vocabulary targets 60% of errors; duplicates are a data fix, short-text a feature.",
    rubric: ["Picks the largest family", "Maps the fix to the family", "Predicts the error reduction"],
    hints: ["Which family dominates?", "What fix matches that family?", "Spend effort where the errors are."],
    pattern: "Largest error family",
    prerequisites: ["EXP-009"],
    nextQuestionId: "EVAL-001",
  }),
  q({
    id: "EVAL-001", track: "experiments", lessonId: "ai-ml/01-baseline/the-baseline-earns-the-right-to-improve",
    kind: "prediction",
    prompt: "Choose metrics for this high-recall moderation system: missing a bad comment costs 50× a false flag. Which metric must be maximized?",
    contextFixture: { costMiss: 50, costFalse: 1 },
    expectedArtifact: "Recall on the bad class first (missing is 50×), then precision to limit false flags — report both, decide by recall.",
    rubric: ["Picks recall", "Justifies from cost", "Keeps precision visible"],
    hints: ["Which error does the product fear most?", "Which metric tracks that error?", "What do you report alongside it?"],
    pattern: "Metric choice",
    prerequisites: ["STAT-003"],
    nextQuestionId: "EVAL-002",
  }),
  q({
    id: "EVAL-002", track: "experiments", lessonId: "ai-ml/01-baseline/the-baseline-earns-the-right-to-improve",
    kind: "derivation",
    prompt: "Calculate PR-AUC from these points: recall (0.2, precision 1.0), (0.5, 0.8), (0.9, 0.5).",
    contextFixture: { points: [[0.2, 1.0], [0.5, 0.8], [0.9, 0.5]] },
    expectedArtifact: "Trapezoid sum ≈ 0.2·1.0 + 0.3·0.9 + 0.4·0.65 ≈ 0.73 — the area under the precision-recall curve.",
    rubric: ["Splits the recall intervals", "Averages precision per interval", "Sums the areas"],
    hints: ["What is the width of each recall interval?", "What precision do you use per interval?", "Sum the trapezoids."],
    pattern: "PR-AUC",
    prerequisites: ["STAT-003"],
    nextQuestionId: "EVAL-003",
  }),
  q({
    id: "EVAL-003", track: "experiments", lessonId: "ai-ml/02-linear-classifier/when-a-good-score-lies",
    kind: "debugging",
    prompt: "A model reports 0.95 accuracy but the 'feature' class recall is 0.1. Find the metric that hides this minority-class failure.",
    contextFixture: { accuracy: 0.95, featureRecall: 0.1, balance: "92/6/2" },
    expectedArtifact: "Accuracy hides it: the 92% majority swamps the score — report per-class recall, especially on the 2% feature class.",
    rubric: ["Names accuracy as the mask", "Explains the majority swamp", "Prescribes per-class metrics"],
    hints: ["How is 0.95 achieved?", "What happens on the tiny class?", "Which number tells the truth?"],
    pattern: "Metric honesty",
    prerequisites: ["STAT-002"],
    nextQuestionId: "EVAL-004",
  }),
  q({
    id: "EVAL-004", track: "experiments", lessonId: "ai-ml/05-failure-lab/find-the-error-family",
    kind: "construction",
    prompt: "Design slice evaluation for these user segments: mobile, desktop, and API-only users.",
    contextFixture: { segments: ["mobile", "desktop", "api"] },
    expectedArtifact: "Compute the full metric set per slice, flag slices below the global average, and gate releases on the worst slice — not the average.",
    rubric: ["Defines slices", "Computes per-slice metrics", "Gates on the worst slice"],
    hints: ["What is the unit of a slice?", "Which slice can quietly fail?", "Release on the worst slice, not the best average."],
    pattern: "Slice evaluation",
    prerequisites: ["EVAL-003"],
    nextQuestionId: "EVAL-005",
  }),
  q({
    id: "EVAL-005", track: "experiments", lessonId: "ai-ml/02-linear-classifier/when-a-good-score-lies",
    kind: "communication",
    prompt: "Interpret this calibration curve: predictions in the 0.9 bin are correct 60% of the time. Write the finding.",
    contextFixture: { bin: "0.9", observed: 0.6 },
    expectedArtifact: "Miscalibrated: 0.9-bin confidence should track ~0.9 correctness; the gap means confidence is not trustworthy as probability.",
    rubric: ["Reads the bin vs observed", "States the gap", "Names the consequence"],
    hints: ["What should the 0.9 bin average?", "What does 0.6 mean?", "Confidence must match frequency."],
    pattern: "Calibration",
    prerequisites: ["STAT-006"],
    nextQuestionId: "EVAL-006",
  }),
  q({
    id: "EVAL-006", track: "experiments", lessonId: "ai-ml/03-experiments/reproducibility-is-part-of-correctness",
    kind: "implementation",
    prompt: "Build a regression fixture for this previous failure: a bug was missed because it contained no bug keywords.",
    contextFixture: { failure: "bug with no keywords", fixture: "12 such rows" },
    expectedArtifact: "A golden set of 12 keyword-free bug rows that every new model must pass — regression tests are data, not vibes.",
    rubric: ["Encodes the past failure", "Is deterministic", "Gates future releases"],
    hints: ["What must never regress?", "How do you freeze the failure?", "A fixture makes regressions checkable."],
    pattern: "Regression fixture",
    prerequisites: ["EXP-004"],
    nextQuestionId: "EVAL-007",
  }),
  q({
    id: "EVAL-007", track: "experiments", lessonId: "ai-ml/04-inference-service/measure-the-service-not-just-the-model",
    kind: "prediction",
    prompt: "Decide whether this model is ready for shadow traffic: offline F1 0.82, regression fixtures pass, calibration ECE 0.04.",
    contextFixture: { f1: 0.82, fixtures: "pass", ece: 0.04 },
    expectedArtifact: "Ready for shadow: the offline bar is met and calibration is sound — shadow lets real traffic vote before any user sees it.",
    rubric: ["Reads the bar as met", "Checks calibration", "Names shadow's role"],
    hints: ["What must hold before serving?", "What does ECE 0.04 buy you?", "Shadow traffic tests reality without risk."],
    pattern: "Shadow evaluation",
    prerequisites: ["EVAL-006"],
    nextQuestionId: "EVAL-008",
  }),
  q({
    id: "EVAL-008", track: "experiments", lessonId: "ai-ml/04-inference-service/measure-the-service-not-just-the-model",
    kind: "debugging",
    prompt: "Latency rose 40% while quality held. Separate data, model, and serving regressions: which bucket does this belong to?",
    contextFixture: { latency: "+40%", quality: "flat" },
    expectedArtifact: "Serving regression (infrastructure), not model or data: quality flat rules out model/data; latency is the transport layer's job.",
    rubric: ["Rules out model change", "Rules out data change", "Attributes to serving"],
    hints: ["Did the model's outputs change?", "Did the inputs change?", "What metric moved — quality or ops?"],
    pattern: "Regression buckets",
    prerequisites: ["EVAL-007"],
    nextQuestionId: "EVAL-009",
  }),
  q({
    id: "EVAL-009", track: "experiments", lessonId: "ai-ml/04-inference-service/measure-the-service-not-just-the-model",
    kind: "communication",
    prompt: "Explain why offline success did not transfer online: offline F1 0.9, live precision 0.6. Write the likely causes.",
    contextFixture: { offline: 0.9, live: 0.6, cause: "distribution shift / annotation gap" },
    expectedArtifact: "A short postmortem: live traffic differs from the training distribution (drift), and offline labels may disagree with real ones — close the loop with live sampling.",
    rubric: ["Names drift", "Names the annotation gap", "Proposes live evaluation"],
    hints: ["What differs between bench and street?", "Who wrote the offline labels?", "Measure where it matters."],
    pattern: "Offline vs online",
    prerequisites: ["EVAL-007"],
    nextQuestionId: "EVAL-010",
  }),
  q({
    id: "EVAL-010", track: "experiments", lessonId: "ai-ml/01-baseline/the-baseline-earns-the-right-to-improve",
    kind: "communication",
    prompt: "Write the acceptance question for this model release: bug recall must beat the baseline on the fixed split, with no slice below 0.5.",
    contextFixture: { gate: "recall > baseline", slice: "≥ 0.5" },
    expectedArtifact: "A one-line acceptance question: 'Does the new model beat the baseline's bug recall on the fixed split with every slice ≥ 0.5?'",
    rubric: ["Names the metric", "Names the comparison", "Names the slice floor"],
    hints: ["What is compared to what?", "Which metric?", "What protects the worst slice?"],
    pattern: "Acceptance question",
    prerequisites: ["BASE-004"],
    nextQuestionId: "TENSOR-001",
  }),
]
